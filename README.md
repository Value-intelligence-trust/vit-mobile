# vit-mobile — VIT Network Telegram Mini App

React + Vite prediction UI for Telegram.

```bash
npm install && cp .env.example .env
npm run dev    # local dev
npm run build  # production build -> dist/
```

Deploy `dist/` to any static host, then set the URL in @BotFather -> /setmenubutton -> Web App URL.
